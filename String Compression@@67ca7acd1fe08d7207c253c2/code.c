// Your code here...
#include<stdio.h>
#include<string.h>
int compressString(char str[100],char compressed){
    int count=0;
    for(int i=0;i<strlen(str[100]);i++){
        count++;
    }
    return str[0]+count;
}